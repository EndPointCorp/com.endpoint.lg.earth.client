<?xml version="1.0"?>
<project type="activity" builder="java">
  <name>Google Earth Client</name>
  <description>
This activity is meant to stop and start a Google Earth client instance. It writes the Google Earth configuration files on startup and configuration change.
  </description>

  <identifyingName>com.endpoint.lg.earth.client</identifyingName>
  <version>1.0.0.dev</version>

  <deployments>
    <deployment type="testdeploy" location="${deployment.test.deploy}" />
    <deployment type="testimport" location="${deployment.test.import}" />
  </deployments>
</project>
